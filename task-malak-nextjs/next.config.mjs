/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['images.unsplash.com', 'tailwindcss.com', 'pexels.com' , 'cdn.dummyjson.com'], 
    },
  };
  
  export default nextConfig;
  